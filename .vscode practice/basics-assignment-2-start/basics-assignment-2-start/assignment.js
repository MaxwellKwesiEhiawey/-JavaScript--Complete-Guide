const task3Element = document.getElementById('task-3');

function firstName() {
  alert('just trying')
}

function surname(name) {
alert('I am' + name);
}

function combine(try1, try2, try3) {
  const combinedText = `${try1} ${try2} ${try3}`;
}
return combinedText;

surname( ' Ehiawey')

task3Element.addEventListener('click' greet);

const combinedString = combine('It is fine', 'trying', 'out!' );
alert(combinedString);
function combine(try1, try2, try3) {
  const combinedText = `${try1} ${try2} ${try3}`;
}
return combinedText;

surname( ' Ehiawey')
task3Element.addEventListener('click', greet);

const combinedString = combine('It is fine', 'trying', 'out!' );
alert(combinedString);