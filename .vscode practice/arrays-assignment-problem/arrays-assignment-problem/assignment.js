const createdArray = [2, 3, 4, 5, 6];

const isBiggerThan5 = createdArray.filter(number => number > 5);
console.log(isBiggerThan5);

const createdArray2 = [ 8, 10]
reversed = createdArray.reverse();
console.log('reversed:', reversed);
removeItem = createdArray.shift();
concatedArray = createdArray.concat(createdArray2)
console.log(removeItem);
console.log(createdArray);
console.log(concatedArray);

